node_modules/
_site/
dist/
.cache/
*.log
_references/
.worktrees/

# Secrets — never commit. Use .env locally, GitHub Actions secrets in CI.
.env
.env.local
.env.*.local

# Personal go-live notes / server runbook — keep off git.
/TODO.md
ssh-hostkey-backup.tgz

# Go build artifacts
tui/sztui
tui/tui
